# Build artifacts
build/
dist/
Spark/
Spark-*.zip

# Secrets locaux
scraper/config.py

# Documents utilisateur (factures PDF importées, générés localement)
documents/

# Exports générés localement (peuvent contenir des données financières réelles du portefeuille)
exports/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
venv/
.env
.worktrees/
.pytest_cache/

# Données locales du scraper
scraper/biens.db
scraper/spark_scraper.log
geocache.json

# Firebase CLI (cache local de deploy, pas de secret dedans mais inutile a versionner)
.firebase/
firebase-debug.log

# Node (tooling de test uniquement — Playwright pour verifier l'app dans un vrai navigateur,
# voir CLAUDE.md section Testing. Deja exclu du deploy Firebase via firebase.json.)
node_modules/
