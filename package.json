{
  "name": "investissement_web",
  "version": "1.0.0",
  "private": true,
  "description": "Outil d'analyse d'investissement immobilier (app desktop) + tests calculs.js + tooling de verification navigateur (Playwright)",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "for f in tests/*.mjs; do node \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  },
  "license": "ISC"
}
