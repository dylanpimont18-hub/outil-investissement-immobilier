# Dépendances de l'application desktop Spark
# Installer avec : pip install -r requirements.txt
# Après pip install : playwright install chromium

flask==3.1.2
pywebview==6.2.1
pystray==0.19.5
pillow==10.4.0
anthropic==0.46.0
playwright>=1.44.0
playwright-stealth>=1.0.6
