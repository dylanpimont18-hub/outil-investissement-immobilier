rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Documents PDF du portefeuille : meme regle que Firestore, scope par uid.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
